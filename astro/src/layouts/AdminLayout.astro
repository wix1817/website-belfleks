---
import { requireAuth, PB_URL } from '../lib/adminAuth';

interface Props {
  title?: string;
  activeNav?: 'dashboard' | 'products' | 'categories' | 'pages' | 'leads' | 'documents' | 'settings';
}

const { title = 'Панель управления | БелФлекс', activeNav = 'dashboard' } = Astro.props;

// Server-side guard
const authResult = requireAuth(Astro);
if (authResult instanceof Response) {
  return authResult;
}

const { manager } = authResult;
const currentPath = Astro.url.pathname;

const navItems = [
  {
    id: 'dashboard',
    label: 'Dashboard',
    href: '/manage/dashboard',
    icon: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/></svg>`
  },
  {
    id: 'products',
    label: 'Товары',
    href: '/manage/products',
    icon: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/></svg>`
  },
  {
    id: 'categories',
    label: 'Категории',
    href: '/manage/categories',
    icon: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7"/></svg>`
  },
  {
    id: 'pages',
    label: 'Страницы',
    href: '/manage/pages',
    icon: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>`
  },
  {
    id: 'leads',
    label: 'Заявки',
    href: '/manage/leads',
    icon: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>`
  },
  {
    id: 'documents',
    label: 'Документы',
    href: '/manage/documents',
    icon: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"/></svg>`
  },
  {
    id: 'settings',
    label: 'Настройки',
    href: '/manage/settings',
    icon: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/></svg>`
  },
];
---

<!doctype html>
<html lang="ru" class="h-full">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/pocketbase@0.21.3/dist/pocketbase.umd.js"></script>
    <style>
      body {
        font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      }
      [x-cloak] { display: none !important; }
    </style>
  </head>
  <body class="h-full bg-[#F7FAFC] text-gray-900 antialiased" x-data="{ sidebarOpen: false }">
    <div class="min-h-full flex flex-col md:flex-row">
      <!-- Backdrop for mobile drawer -->
      <div
        x-show="sidebarOpen"
        x-cloak
        @click="sidebarOpen = false"
        x-transition:enter="transition-opacity ease-linear duration-300"
        x-transition:enter-start="opacity-0"
        x-transition:enter-end="opacity-100"
        x-transition:leave="transition-opacity ease-linear duration-300"
        x-transition:leave-start="opacity-100"
        x-transition:leave-end="opacity-0"
        class="fixed inset-0 bg-black/60 z-40 md:hidden"
      ></div>

      <!-- Left Sidebar (Desktop & Mobile Drawer) -->
      <aside
        :class="sidebarOpen ? 'translate-x-0' : '-translate-x-full md:translate-x-0'"
        class="fixed top-0 bottom-0 left-0 z-50 w-[260px] bg-[#1a1a1a] text-gray-300 flex flex-col transition-transform duration-300 ease-in-out md:static md:translate-x-0 flex-shrink-0"
      >
        <!-- Sidebar Brand / Logo -->
        <div class="h-16 px-6 flex items-center justify-between border-b border-white/10">
          <a href="/manage/dashboard" class="flex items-center gap-3 group">
            <img src="/logo_full.svg" alt="БелФлекс" class="h-7 w-auto brightness-200" />
            <span class="text-xs font-semibold uppercase tracking-wider text-gray-400 bg-white/10 px-2 py-0.5 rounded">Admin</span>
          </a>
          <button
            type="button"
            @click="sidebarOpen = false"
            class="md:hidden text-gray-400 hover:text-white p-1"
            aria-label="Закрыть меню"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
          </button>
        </div>

        <!-- Navigation items -->
        <nav class="flex-1 px-3 py-4 space-y-1.5 overflow-y-auto">
          {navItems.map((item) => {
            const isActive = activeNav === item.id || currentPath === item.href;
            return (
              <a
                href={item.href}
                class:list={[
                  'flex items-center gap-3.5 px-4 py-3 rounded-xl text-sm font-medium transition-all duration-200',
                  isActive
                    ? 'bg-[#EF1C24] text-white shadow-md font-semibold'
                    : 'text-gray-300 hover:bg-white/5 hover:text-white'
                ]}
              >
                <span class:list={['flex-shrink-0', isActive ? 'text-white' : 'text-gray-400']}>
                  <Fragment set:html={item.icon} />
                </span>
                <span>{item.label}</span>
              </a>
            );
          })}
        </nav>

        <!-- Sidebar footer: link to site -->
        <div class="p-4 border-t border-white/10">
          <a
            href="/"
            target="_blank"
            class="flex items-center justify-center gap-2 w-full py-2.5 px-3 rounded-lg text-xs font-medium text-gray-400 hover:text-white hover:bg-white/5 transition-colors border border-white/10"
          >
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/></svg>
            Перейти на сайт
          </a>
        </div>
      </aside>

      <!-- Main Layout: Top Bar + Content -->
      <div class="flex-1 flex flex-col min-w-0 md:pl-0">
        <!-- Top Bar (Height 64px) -->
        <header class="h-16 bg-white border-b border-gray-200 px-4 sm:px-6 lg:px-8 flex items-center justify-between sticky top-0 z-30 shadow-xs">
          <div class="flex items-center gap-3">
            <!-- Mobile burger button -->
            <button
              type="button"
              @click="sidebarOpen = true"
              class="md:hidden p-2 rounded-lg text-gray-600 hover:text-gray-900 hover:bg-gray-100 transition-colors"
              aria-label="Открыть меню"
            >
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
            </button>
            <div class="hidden sm:block">
              <span class="text-sm font-medium text-gray-500">Добро пожаловать,</span>
              <span class="text-sm font-semibold text-gray-800 ml-1">{manager.name || manager.email}</span>
            </div>
          </div>

          <div class="flex items-center gap-4">
            <!-- Manager badge -->
            <div class="hidden md:flex items-center gap-2.5 pl-3 pr-1 py-1 rounded-full bg-gray-50 border border-gray-200">
              <div class="w-7 h-7 rounded-full bg-[#EF1C24]/10 text-[#EF1C24] flex items-center justify-center font-bold text-xs">
                {(manager.name?.[0] || manager.email[0]).toUpperCase()}
              </div>
              <span class="text-xs font-medium text-gray-700 pr-2">{manager.email}</span>
            </div>

            <!-- Logout button -->
            <button
              type="button"
              onclick="handleLogout()"
              class="inline-flex items-center gap-2 py-2 px-3.5 rounded-lg text-xs font-semibold text-gray-700 hover:text-[#EF1C24] hover:bg-red-50 border border-gray-200 hover:border-red-200 transition-colors cursor-pointer"
            >
              <svg class="w-4 h-4 text-gray-500 group-hover:text-[#EF1C24]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
              </svg>
              <span>Выход</span>
            </button>
          </div>
        </header>

        <!-- Main Content Area -->
        <main class="flex-1 p-4 sm:p-6 lg:p-8 bg-[#F7FAFC]">
          <slot />
        </main>
      </div>
    </div>

    <!-- Client-side Auth & Logout Handler -->
    <script define:vars={{ PB_URL }}>
      window.handleLogout = function() {
        try {
          if (window.PocketBase) {
            const pb = new window.PocketBase(PB_URL);
            pb.authStore.clear();
          }
        } catch (e) {}
        document.cookie = 'pb_auth=; Path=/; Expires=Thu, 01 Jan 1970 00:00:01 GMT; SameSite=Lax;';
        localStorage.removeItem('pocketbase_auth');
        sessionStorage.removeItem('pocketbase_auth');
        window.location.href = '/manage/';
      };

      // Client-side session verification safeguard
      (function checkClientAuth() {
        const cookieMatch = document.cookie.match(/(^|;)\s*pb_auth=([^;]+)/);
        if (!cookieMatch) {
          window.location.href = '/manage/';
        }
      })();
    </script>
  </body>
</html>
